{
  "name": "pi-remote-attachments",
  "version": "0.1.0",
  "description": "Pull files into Pi from an SSH source over OpenSSH SFTP",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi",
    "ssh",
    "sftp",
    "attachments"
  ],
  "engines": {
    "node": ">=22.19.0"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.ts"
  }
}
